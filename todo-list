		short s = 123;
        for (int i=0; i<2; ++i) {
			printf("%x ", ((char*)(&s))[i]);
		}
        printf("done...\n");



		for (size_t i=0; i<length; ++i) {
			printf("%x ",(data[i]));
		}
        printf("\n");
